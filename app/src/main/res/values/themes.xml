<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StreamVerse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StreamVerse" parent="Base.Theme.StreamVerse" />

    <!-- Full Dialog Theme for Add Content -->
    <style name="Theme.StreamVerse.FullDialog" parent="Theme.MaterialComponents.Dialog">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <!-- Rounded Thumbnail for ShapeableImageView -->
    <style name="RoundedThumbnail" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">14dp</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.StreamVerse.Splash" parent="Theme.StreamVerse">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
    </style>

</resources>